{
  "name": "surveys",
  "version": "1.4.14",
  "baseBundleVersion": "0.59.3",
  "private": true,
  "scripts": {
    "build": "pushd mobile-web && npm run build && popd",
    "start": "pushd mobile && npm run ios && popd && pushd web/admin && npm start && popd"
  },
  "doubledutch": {
    "extension": true
  }
}
